name: Run tests

on:
  push:
    branches: [ "**" ]

jobs:
  test_backend:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - name: Setup .NET
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: 10.0.x
        
    - name: Install dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build
    - name: Print all files in Lambda.ListConcerts
      run: ls -R Lambda.ListConcerts
    - name: Test with dotnet
      # TODO: Fix runsettings
      run: dotnet test --logger trx --results-directory "TestResults" --settings:"Lambda.GetS3UploadUrl/test/Lambda.GetS3UploadUrl.Tests/test.runsettings"
    - name: Upload dotnet test results
      uses: actions/upload-artifact@v7
      with:
        name: dotnet-results
        path: TestResults
      # Use always() to always run this step to publish test results when there are test failures
      if: ${{ always() }}
    - name: Generate test report
      uses: bibipkins/dotnet-test-reporter@v1.4.0
      if: ${{ always() }}
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        comment-title: 'Unit Test Results'
        results-path: ./TestResults/*.trx
