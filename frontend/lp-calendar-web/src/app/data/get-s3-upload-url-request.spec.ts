import { GetS3UploadUrlRequest } from './get-s3-upload-url-request';

describe('GetS3UploadUrlRequest', () => {
  it('should create an instance', () => {
    expect(new GetS3UploadUrlRequest()).toBeTruthy();
  });
});
