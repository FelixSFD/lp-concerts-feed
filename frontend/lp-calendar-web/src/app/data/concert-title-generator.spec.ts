import { ConcertTitleGenerator } from './concert-title-generator';

describe('ConcertTitleGenerator', () => {
  it('should create an instance', () => {
    expect(new ConcertTitleGenerator()).toBeTruthy();
  });
});
