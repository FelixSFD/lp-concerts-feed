import { DeleteConcertRequest } from './delete-concert-request';

describe('DeleteConcertRequest', () => {
  it('should create an instance', () => {
    expect(new DeleteConcertRequest()).toBeTruthy();
  });
});
